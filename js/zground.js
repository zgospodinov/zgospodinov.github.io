function myResizeCallback() {
    // var w = window.outerWidth;
    // var h = window.outerHeight;
    // var txt = "Window size: width=" + w + ", height=" + h;
    // console.log(txt)

    var clHeight = window.document.clientHeight;
    console.log(clHeight);
    
};